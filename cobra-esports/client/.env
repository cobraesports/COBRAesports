# Адрес backend-сервиса на Render. Используется на этапе сборки (npm run build) —
# см. client/src/config/api.ts. Локальный npm run dev эту переменную игнорирует
# (там используется Vite dev-proxy на http://localhost:5000, см. vite.config.ts).
VITE_API_URL=https://cobra-esports-1.onrender.com
